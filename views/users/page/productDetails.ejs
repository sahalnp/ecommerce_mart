<%- include('../partials/header') %>
<style>
    /* General product page styles */
    .product-detail-page {
        background: #fff;
        padding: 2rem 0;
    }

    .product-gallery {
        position: relative;
        padding-right: 1rem;
    }

    .product-info {
        padding-left: 2rem;
    }

    .price-section,
    .product-specs {
        margin: 1.5rem 0;
        padding: 1rem;
        background: #fafafa;
        border-radius: 4px;
    }

    .current-price {
        font-size: 28px;
        color: #b12704;
        margin-right: 1rem;
    }

    .original-price {
        font-size: 18px;
        color: #565959;
    }

    .savings-badge {
        background: #cc0c39;
        color: white;
        padding: 3px 7px;
        border-radius: 3px;
        font-size: 14px;
    }

    .stock-status {
        color: #007600;
        font-size: 18px;
        margin: 1rem 0;
        font-weight: bold;
    }

    .text-danger {
        color: #cc0c39;
        font-size: 18px;
        font-weight: bold;
    }

    .quantity-control {
        display: flex;
        align-items: center;
        margin: 1.5rem 0;
    }

    .quantity-input {
        width: 60px;
        text-align: center;
        border: 1px solid #d5d9d9;
        border-radius: 4px;
        padding: 5px;
        margin: 0 10px;
    }

    .btn-qty {
        background: #f0f2f2;
        border: 1px solid #d5d9d9;
        padding: 5px 15px;
        border-radius: 4px;
        cursor: pointer;
    }

    .btn-qty:hover {
        background: #e3e6e6;
    }

    .action-buttons {
        display: flex;
        gap: 1rem;
        margin-top: 2rem;
    }

    .btn-add-to-cart,
    .btn-buy-now {
        padding: 10px 50px;
        border-radius: 20px;
        color: #0f1111;
    }

    .btn-add-to-cart {
        background: #ffd814;
        border-color: #fcd200;
    }

    .btn-buy-now {
        background: #ffa41c;
        border-color: #ff8f00;
    }

    .cart-toast {
        position: fixed;
        bottom: 20px;
        right: 20px;
        background: #28a745;
        color: white;
        padding: 15px 25px;
        border-radius: 5px;
        display: none;
        animation: slideIn 0.5s, fadeOut 0.5s 2.5s;
    }

    @keyframes slideIn {
        from {
            right: -100px;
            opacity: 0;
        }
        to {
            right: 20px;
            opacity: 1;
        }
    }

    @keyframes fadeOut {
        from {
            opacity: 1;
        }
        to {
            opacity: 0;
        }
    }

    .product-specs .specs-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
    }

    .spec-item {
        font-size: 14px;
        line-height: 1.5;
    }

    .spec-label {
        color: #565959;
        font-weight: 500;
    }

    /* Zoom image styles */
    .zoom-wrapper {
        position: relative;
        overflow: hidden;
        display: inline-block;
        width: 400px; /* or any fixed width you want */
        height: 400px;
    }

    .zoom-inner {
        width: 100%;
        height: 100%;
        transition: transform 0.1s ease;
        object-fit: contain;
    }

    .zoom-container {
        position: relative;
        overflow: hidden;
    }

    .zoom-img {
        width: 100%;
        max-height: 450px;
        object-fit: contain;
        display: block;
    }

    .zoom-lens {
        position: absolute;
        border: 1px solid #d4d4d4;
        width: 100px;
        height: 100px;
        visibility: hidden;
        pointer-events: none;
    }

    .zoom-result {
        position: absolute;
        top: 0;
        left: 105%;
        width: 400px;
        height: 400px;
        border: 1px solid #d4d4d4;
        background-repeat: no-repeat;
        background-size: 200% 200%;
        display: none;
        z-index: 100;
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
        .product-info {
            padding-left: 0;
            margin-top: 2rem;
        }

        .action-buttons {
            flex-direction: column;
        }

        .specs-grid {
            grid-template-columns: 1fr;
        }
    }
</style>

<main class="product-detail-page">
    <div class="container my-5">
        <div class="row gx-4 align-items-start">
            <!-- Image Slider -->
            <div class="col-md-6">
                <div class="swiper mainSwiper">
                    <div class="swiper-wrapper">
                        <% for(let i = 0; i < product.image.length; i++) { %>

                        <div class="swiper-slide">
                            <div class="zoom-wrapper">
                                <img
                                    id="zoomImage"
                                    src="<%= product.image[i].filepath %>"
                                    class="zoom-inner"
                                />
                            </div>
                        </div>
                        <% } %>
                    </div>
                </div>

                <!-- Thumbnail Swiper -->
                <div class="swiper thumbSwiper mt-3">
                    <div class="swiper-wrapper">
                        <% for(let i = 0; i < product.image.length; i++) { %>
                        <div class="swiper-slide">
                            <img
                                src="<%= product.image[i].filepath %>"
                                class="img-thumbnail"
                                style="max-height: 100px; object-fit: contain"
                            />
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- Product Info -->
            <div class="col-lg-6 product-info">
                <h1 class="product-title mb-3"><%= product.title %></h1>

                <div class="rating mb-3">
                    <span class="text-warning">★★★★☆</span>
                    <span class="text-muted">(1,234 ratings)</span>
                </div>

                <div class="price-section">
                    <span class="current-price"
                        >₹<%= product.pricing.salePrice %></span
                    >
                    <span class="original-price"
                        ><del>₹<%= product.pricing.price %></del></span
                    >
                    <span class="savings-badge">Save <%= discount %>%</span>
                </div>

                <% if (product.inStock > 0) { %>
                <div class="stock-status">In Stock</div>
                
                <% } else { %>
                <div class="text-danger">Out of stock</div>
                <% } %>

                <div class="product-highlights">
                    <ul class="list-unstyled custom-list">
                        <li>✓ Free delivery by Friday, May 9</li>
                        <li>✓ <%= product.warranty %> warranty</li>
                        <li>✓ 7-day returns policy</li>
                    </ul>
                </div>
                
                <form action="/product/<%= product._id %>" method="POST">
                     <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <input
                        type="hidden"
                        name="productId"
                        value="<%= product._id %>"
                    />

                    <div class="quantity-control">
                        <label
                            for="quantity"
                            class="mb-0 mr-2 font-weight-medium"
                            >Quantity:</label
                        >

                        <button
                            class="btn-qty btn-decrease"
                            style="color: #0f1111"
                            type="button"
                        >
                            −
                        </button>

                        <input
                            type="number"
                            id="quantity"
                            class="quantity-input"
                            name="quantity"
                            value="2"
                            min="1"
                            max="10"
                        />

                        <button
                            class="btn-qty btn-increase"
                            style="color: #0f1111"
                            type="button"
                        >
                            +
                        </button>
                    </div>

                  <div class="action-buttons">
    <% if (exist == true) { %>
        <!-- Just a link, no form submission -->
        <a href="/cart" class="btn btn-add-to-cart">
            <i class="fas fa-shopping-cart me-2"></i> Go to cart
        </a>
    <% } else { %>
        <!-- This button will submit the form to add the product to cart -->
        <button type="submit" class="btn btn-add-to-cart">
            <i class="fas fa-shopping-cart me-2"></i> <%= cart %>
        </button>
    <% } %>

    <button type="button" class="btn btn-buy-now">
        <i class="fas fa-bolt"></i> Buy Now
    </button>

    <div class="cart-toast">Item added to cart! 🎉</div>
</div>

                </form>

                <div class="product-specs">
                    <h5 class="mb-3">Product Specifications</h5>
                    <div class="specs-grid">
                        <div class="spec-item">
                            <span class="spec-label">Brand:</span>&nbsp <%=
                            product.brand.name %>
                        </div>
                        <div class="spec-item">
                            <span class="spec-label">Band Material:</span>&nbsp
                            <%= product.bandMaterial %>
                        </div>
                        <div class="spec-item">
                            <span class="spec-label">Case Diameter:</span>
                            &nbsp<%= product.caseDiameter %>
                        </div>
                        <div class="spec-item">
                            <span class="spec-label">Band Color:</span>&nbsp <%=
                            product.bandColour %>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Product Description -->
        <div class="row mt-5">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">About this item</h4>
                        <div class="card-text">
                            <%= product.description %>
                            <ul class="mt-3">
                                <li>
                                    Premium <%= product.bandMaterial %> band
                                    material
                                </li>
                                <li>
                                    <%= product.waterResistance %> water
                                    resistance
                                </li>
                                <li>
                                    <%= product.warranty %> manufacturer
                                    warranty
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>
<script src="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.js"></script>

<script>
    // Initialize Swiper thumbnail and main sliders
    const length = <%= product.image.length %>;
  
    const thumbSwiper = new Swiper(".thumbSwiper", {
      spaceBetween: 10,
      slidesPerView: length,
      watchSlidesProgress: true,
    });
  
    const mainSwiper = new Swiper(".mainSwiper", {
      spaceBetween: 10,
      loop: true,
      thumbs: {
        swiper: thumbSwiper,
      },
    });
  
    // Quantity control
    const decreaseBtn = document.querySelector('.btn-decrease');
    const increaseBtn = document.querySelector('.btn-increase');
    const quantityInput = document.getElementById('quantity');
  
    if (quantityInput) {
      const min = parseInt(quantityInput.min) || 1;
      const max = parseInt(quantityInput.max) || 10;
  
      decreaseBtn.addEventListener('click', () => {
        let value = parseInt(quantityInput.value) || min;
        if (value > min) quantityInput.value = value - 1;
      });
  
      increaseBtn.addEventListener('click', () => {
        let value = parseInt(quantityInput.value) || min;
        if (value < max) quantityInput.value = value + 1;
      });
    }
  
    // Zoom effect on product images
    const zoomWrappers = document.querySelectorAll(".zoom-wrapper");
  
    zoomWrappers.forEach(zoomWrapper => {
      const zoomImage = zoomWrapper.querySelector(".zoom-inner");
  
      zoomWrapper.addEventListener("mousemove", function (e) {
        const rect = zoomWrapper.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;
        const xPercent = (x / rect.width) * 100;
        const yPercent = (y / rect.height) * 100;
  
        zoomImage.style.transformOrigin = `${xPercent}% ${yPercent}%`;
        zoomImage.style.transform = "scale(2)";
      });
  
      zoomWrapper.addEventListener("mouseleave", function () {
        zoomImage.style.transform = "scale(1)";
      });
    });
  

    function addToCart() {
  const toast = document.querySelector('.cart-toast');
  if (toast) {
    toast.style.display = 'block';

    setTimeout(() => {
      toast.style.display = 'none';
    }, 3000);
  }
}

  </script>
  

<%- include('../partials/footer') %>
